---
applyTo: "**"
---

# sage

**Sage cognition pipeline — run before any non-trivial code change**

**Use when:** the user runs `/sage`, or asks for the task above.

Thin adapter — the command body lives in **one** place:
**`agents/sage/commands/sage.md`**. Read that file and follow it verbatim.
Do not duplicate its steps here; edit the canonical file to change behaviour.
