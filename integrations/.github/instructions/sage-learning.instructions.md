---
applyTo: "**"
---

# sage-learning

**Sage Learning — learn this codebase's patterns into agents/sage/**

**Use when:** the user runs `/sage-learning`, or asks for the task above.

Thin adapter — the command body lives in **one** place:
**`agents/sage/commands/sage-learning.md`**. Read that file and follow it verbatim.
Do not duplicate its steps here; edit the canonical file to change behaviour.
