# sage-unit-test

**Sage Unit Test — write unit tests that match how this repo already tests**

**Use when:** the user runs `/sage-unit-test`, or asks for the task above.

Thin adapter — the command body lives in **one** place:
**`agents/sage/commands/sage-unit-test.md`**. Read that file and follow it verbatim.
Do not duplicate its steps here; edit the canonical file to change behaviour.
