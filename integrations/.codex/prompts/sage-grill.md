# sage-grill

**Resolve single-session fog with glossary updates and durable checkpoints**

**Use when:** the user runs `/sage-grill`, or Sage routes the request as
`foggy-single-session`.

Thin adapter — the command body lives in **one** place:
**`agents/sage/commands/sage-grill.md`**. Read that file and follow it verbatim.
Do not duplicate its steps here; edit the canonical file to change behaviour.
