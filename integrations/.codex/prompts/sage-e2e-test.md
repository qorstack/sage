# sage-e2e-test

**Sage E2E Test — drive the real app end-to-end and prove the flow works**

**Use when:** the user runs `/sage-e2e-test`, or asks for the task above.

Thin adapter — the command body lives in **one** place:
**`agents/sage/commands/sage-e2e-test.md`**. Read that file and follow it verbatim.
Do not duplicate its steps here; edit the canonical file to change behaviour.
