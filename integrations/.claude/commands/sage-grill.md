# sage-grill

**Interrogate a foggy request into agreed decisions — before flow or code**

**Use when:** the user runs `/sage-grill`, or the request is ambiguous / has two
defensible designs / hides an unstated trade-off and needs sharpening first.

Thin adapter — the command body lives in **one** place:
**`agents/sage/commands/sage-grill.md`**. Read that file and follow it verbatim.
Do not duplicate its steps here; edit the canonical file to change behaviour.
