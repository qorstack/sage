# sage-wayfinder

**Chart and resolve multi-session fog as a durable map of decision tickets**

**Use when:** the user runs `/sage-wayfinder`, or Sage routes the request as
`large-multi-session`.

Thin adapter — the command body lives in **one** place:
**`agents/sage/commands/sage-wayfinder.md`**. Read that file and follow it verbatim.
Do not duplicate its steps here; edit the canonical file to change behaviour.
