# sage-security-review

**Sage Security Review — review a change for real, exploitable security holes**

**Use when:** the user runs `/sage-security-review`, or asks for the task above.

Thin adapter — the command body lives in **one** place:
**`agents/sage/commands/sage-security-review.md`**. Read that file and follow it verbatim.
Do not duplicate its steps here; edit the canonical file to change behaviour.
