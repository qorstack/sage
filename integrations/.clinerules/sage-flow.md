# sage-flow

**Sage Flow — turn a feature/journey into an implementation-ready business flow**

**Use when:** the user runs `/sage-flow`, or asks for the task above.

Thin adapter — the command body lives in **one** place:
**`agents/sage/commands/sage-flow.md`**. Read that file and follow it verbatim.
Do not duplicate its steps here; edit the canonical file to change behaviour.
