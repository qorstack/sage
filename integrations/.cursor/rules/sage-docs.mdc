---
description: Sage Docs — create or update a plain-Markdown flow doc in docs/
alwaysApply: false
---

# sage-docs

**Sage Docs — create or update a plain-Markdown flow doc in docs/**

**Use when:** the user runs `/sage-docs`, or asks for the task above.

Thin adapter — the command body lives in **one** place:
**`agents/sage/commands/sage-docs.md`**. Read that file and follow it verbatim.
Do not duplicate its steps here; edit the canonical file to change behaviour.
