---
description: Sage N2N Test — drive the real app end-to-end and prove the flow works
alwaysApply: false
---

# sage-n2n-test

**Sage N2N Test — drive the real app end-to-end and prove the flow works**

**Use when:** the user runs `/sage-n2n-test`, or asks for the task above.

Thin adapter — the command body lives in **one** place:
**`agents/sage/commands/sage-n2n-test.md`**. Read that file and follow it verbatim.
Do not duplicate its steps here; edit the canonical file to change behaviour.
