---
description: Chart multi-session fog as a durable map of decision tickets
alwaysApply: false
---

# sage-wayfinder

**Use when:** the user runs `/sage-wayfinder`, or Sage routes the request as
`large-multi-session`.

Thin adapter — read and follow **`agents/sage/commands/sage-wayfinder.md`** verbatim.
Do not duplicate its steps here; edit the canonical file to change behaviour.
