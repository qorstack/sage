POSTGRES_HOST=localhost
# Non-standard host port — avoid clashing with a system Postgres on 5432.
POSTGRES_PORT=55432
POSTGRES_USER=precept
POSTGRES_PASSWORD=precept
POSTGRES_DB=precept
POSTGRES_SCHEMA=public

# Dashboard host port — far from the crowded 8080 range.
WEB_PORT=9080
